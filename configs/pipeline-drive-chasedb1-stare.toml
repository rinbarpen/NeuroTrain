[drive]
config = "configs/train-drive.toml"
ext_args = ["--train", "--test", "--predict"]

[stare]
config = "configs/train-stare.toml"
ext_args = ["--train", "--test", "--predict"]

[chasedb1]
config = "configs/train-chasedb1.toml"
ext_args = ["--train", "--test", "--predict"]
